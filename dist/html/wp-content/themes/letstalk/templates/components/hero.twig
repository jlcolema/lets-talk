<div class="hero">

	{% set hero = Image(location.location_welcome_hero) %}

	<img src="{{ hero.src }}" srcset="" sizes="" width="" height="" decoding="async" alt="{{ hero.alt }}" class="hero__img" />

</div>